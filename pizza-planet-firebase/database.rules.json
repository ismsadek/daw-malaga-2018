/*{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null"
  }
}*/
{
  "rules": {
    ".read": true,
    ".write": true,
    "menu":{
      // ".validate": "newData.hasChildren(['name', 'description', 'options']) && newData.child('name').isString() && newData.child('description').isString() && newData.child('description').val().length < 500"
    }
  }
}